<h1>Listing questions</h1>

<table>
  <tr>
    <th>Title</th>
    <th>Text</th>
    <th>Variant</th>
    <th>Is published</th>
    <th>Type</th>
    <th>Sort key</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @questions.each do |question| %>
  <tr>
    <td><%= question.title %></td>
    <td><%= question.text %></td>
    <td><%= question.variant %></td>
    <td><%= question.is_published %></td>
    <td><%= question.type %></td>
    <td><%= question.sort_key %></td>
    <td><%= link_to 'Show', question %></td>
    <td><%# link_to 'Edit', edit_admin_question_path(question) %></td>
    <td><%= link_to 'Destroy', question, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Question', new_admin_question_path %>
